package com.example.aadharscanner.database;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "SHG_LOAN_STATUS_DATA".
*/
public class ShgLoanStatusDataDao extends AbstractDao<ShgLoanStatusData, Void> {

    public static final String TABLENAME = "SHG_LOAN_STATUS_DATA";

    /**
     * Properties of entity ShgLoanStatusData.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property LoanStatus = new Property(0, String.class, "loanStatus", false, "LOAN_STATUS");
        public final static Property LoanStatusId = new Property(1, String.class, "loanStatusId", false, "LOAN_STATUS_ID");
    }


    public ShgLoanStatusDataDao(DaoConfig config) {
        super(config);
    }
    
    public ShgLoanStatusDataDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"SHG_LOAN_STATUS_DATA\" (" + //
                "\"LOAN_STATUS\" TEXT," + // 0: loanStatus
                "\"LOAN_STATUS_ID\" TEXT);"); // 1: loanStatusId
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"SHG_LOAN_STATUS_DATA\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, ShgLoanStatusData entity) {
        stmt.clearBindings();
 
        String loanStatus = entity.getLoanStatus();
        if (loanStatus != null) {
            stmt.bindString(1, loanStatus);
        }
 
        String loanStatusId = entity.getLoanStatusId();
        if (loanStatusId != null) {
            stmt.bindString(2, loanStatusId);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, ShgLoanStatusData entity) {
        stmt.clearBindings();
 
        String loanStatus = entity.getLoanStatus();
        if (loanStatus != null) {
            stmt.bindString(1, loanStatus);
        }
 
        String loanStatusId = entity.getLoanStatusId();
        if (loanStatusId != null) {
            stmt.bindString(2, loanStatusId);
        }
    }

    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    @Override
    public ShgLoanStatusData readEntity(Cursor cursor, int offset) {
        ShgLoanStatusData entity = new ShgLoanStatusData( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // loanStatus
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1) // loanStatusId
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, ShgLoanStatusData entity, int offset) {
        entity.setLoanStatus(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setLoanStatusId(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
     }
    
    @Override
    protected final Void updateKeyAfterInsert(ShgLoanStatusData entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    @Override
    public Void getKey(ShgLoanStatusData entity) {
        return null;
    }

    @Override
    public boolean hasKey(ShgLoanStatusData entity) {
        // TODO
        return false;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
